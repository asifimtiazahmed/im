import 'package:flutter/material.dart';

class IMTextInput extends StatelessWidget {
  const IMTextInput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
